<form method="post" action="{{ path('app_paniers_articles_delete', {'id': paniers_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paniers_article.id) }}">
    <button class="btn">Delete</button>
</form>
