{% extends 'base.html.twig' %}

{% block title %}Commercants{% endblock %}

{% block body %}
    <h1>Commercants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commercant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ commercant.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ commercant.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commercants_index') }}">back to list</a>

    <a href="{{ path('app_commercants_edit', {'id': commercant.id}) }}">edit</a>

    {{ include('commercants/_delete_form.html.twig') }}
{% endblock %}
